// Site-wide feature switches. The storefront is being repositioned as a B2B
// wholesale site, so the consumer-facing "design your own made-to-measure
// curtain" flow is turned off everywhere it can be reached from — nav menus,
// homepage promo, product cards/links, and the product detail page itself —
// without deleting any of that code. Flip this back to `true` to restore it.
export const CUSTOM_CURTAIN_ENABLED = false;
